{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwind -i ./main.css -o ../resources/static/main.css",
    "watch": "tailwind -i ./main.css --watch",
    "dev": "npm run build && npm run watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}
